<%= render(:partial => "/_entry/entry",
           :locals => {
             :entry => @user,
             :title => user_link(@user, true)
           }) %>

<div class="navi">
  <ul>
    <li><%= link_to s_('Menu|Back'), :action => 'index' %></li>
  </ul>
</div>
